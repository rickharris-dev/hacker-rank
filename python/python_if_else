#!/usr/bin/python

import sys

n = int(raw_input().strip())

if n % 2 == 0 and n in range(6,21):
	sys.stdout.write('Weird')
elif n % 2 == 1:
	sys.stdout.write('Weird')
else:
	sys.stdout.write('Not Weird')
